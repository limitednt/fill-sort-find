# fill-sort-find

accidently deleted the original, basically a code that fils an array the size of "user input" with random numbers, lets the user pick the type of sorting they want(so far: bubble , selection , insertion, merge and quick) sorts, calculates the time it took to run, then saves the values(array values) to another code responsible for binary search, lets the user input the key to finally give the position of the wanted number.



