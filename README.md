# fill-sort-find
accidently deleted the original, bisically a code that fils an array the size of "user input" with random numbers, lets the user pick the type of sorting they want(so far: bubble , selection,insertion and merge) sorts then saves the values to another code responsible for binary search, lets the user input the key to finaly give the position of the wanted number.
