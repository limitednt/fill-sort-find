# fill-sort-find
accidently deleted the original, bisically a code that fild an array the size of user input with random numbers, lets the user pick the type of sorting they want, sorts then saves the values to another code risponsible for binary search, lets the user input the key to finnaly tell the position of the wanted number
